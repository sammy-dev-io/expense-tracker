# Copy this file, rename the copy to ".env", then fill in your real value.

# Locally, this points to your own backend running on your machine.
# Once deployed, you'll set this in your hosting platform's environment
# variables to point at your REAL deployed backend URL instead.
VITE_API_URL=http://localhost:5000/api
